function addItems(n) {
    return n + n
}

// Nesse caso, temos apenas uma operação, mesmo que adicionassemos mais operações de soma, a complexidade desse algoritmo é O(1) pelo mecanismo da simplificação de constantes, pois o número de operações não muda conforme N muda
// Tambem conhecido como constant time
// Mais performático existente